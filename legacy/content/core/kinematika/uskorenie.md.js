/* ПЕРЕПИСАН ПОД РАМКУ 04.09 (бриф lekciya02_faza2_glava.md, пакет П4).
   К теху 03_5_acceleration.tex добавлено: среднее ускорение отдельной
   ступенью с формулой (#srednee); вторая производная и покоординатная
   запись столбцом (#vtoraya, #stolbec) — выполняет обещание
   radius-vektor#stolbec; обратный ход от a(t) к v(t) тем же интегралом
   (#obratnyj; переменная интегрирования t', как в integrirovanie-skorosti,
   правка по аудиту 05.09). Формулы теха (#opredelenie, #proizvodnaya) сохранены
   дословно; a = dv/dt в техе была словами. Числа «100 км/ч за 5 с» и
   пример столбца x = 2t, y = 3t² — мои, в техе их нет. Авторские фразы из
   транскриптов: «ускорение относится к скорости так же, как скорость
   относится к перемещению» (2024, стр. 903), «ровно та же идея» (2025,
   стр. 335), «прикручена болтами» (2025, стр. 241). Снятые обороты —
   в CONVERSION-LOG.md, раздел «Правки 04.09» (ведёт сборщик).
   ПРАВКА 08.09 (замечание автора): три столбца в #stolbec поставлены в
   строку через \qquad вместо трёх строк aligned. Стопкой формула занимала
   397 пикселей высоты, и слайд 34 колоды вылезал за холст на 135 — 855
   против 720. Смысл не меняется, порядок r, v, a сохранён.
   ПРАВКА ПО ФИНАЛЬНОМУ АУДИТУ, КРУГ 1 (05.09): скобочное «см.» у ссылки на
   radius-vektor снято.
   ДЕСЛОПИНГ 06.09: снято 11 конструкций «тезис, двоеточие, продолжение» и
   1 ярлык с двоеточием («Размерность:»), а также рубленое «Пример.» и
   двоеточие из заголовка «Пойдём обратно от ускорения к скорости»;
   содержание не менялось. */
window.BLOCK(String.raw`
---
id: uskorenie
type: вывод
title: Ускорение: как меняется скорость
links: [skorost, integrirovanie-skorosti, radius-vektor, sistema-otscheta, ravnouskorennoe-dvizhenie, godograf, normalnoe-i-tangencialnoe]
---

Скорость описывает, как меняется положение точки со временем. А как меняется сама скорость? За это отвечает ускорение. Новой договорённости здесь не понадобится, потому что описание не сужается и ничего не выбрасывается. То же действие, которым из радиус-вектора получена скорость, применяется ещё раз, к другому вектору. Если скорость точки меняется, значит, у неё есть ускорение.

Со скоростью мы шли в два шага: изменение положения, делённое на промежуток времени, дало среднюю скорость, а предел при стягивании промежутка дал мгновенную. Пройдём те же два шага для скорости.

## Усредним изменение скорости

Пусть за промежуток времени $\Delta t$ скорость изменилась на $\Delta \vec{v} = \vec{v}(t + \Delta t) - \vec{v}(t)$. Среднее ускорение на этом промежутке есть отношение изменения скорости к промежутку:

$$#srednee
\vec{a}_{\mathrm{avg}} = \frac{\Delta \vec{v}}{\Delta t} = \frac{\vec{v}(t + \Delta t) - \vec{v}(t)}{\Delta t}
$$

Это вектор, потому что разность двух векторов делится на число. Величина рабочая, ею пользуются без всякого предела. Автомобиль разогнался с места до 100 км/ч за 5 с. Скорость изменилась на $27{,}8$ м/с, среднее ускорение равно $27{,}8/5 \approx 5{,}6$ м/с$^2$, чуть больше половины ускорения свободного падения. Как менялось ускорение внутри этих пяти секунд, среднее не говорит.

## Стянем промежуток к нулю

Чтобы получить ускорение в данный момент, промежуток $\Delta t$ стягивается к нулю, как при переходе от средней скорости к мгновенной:

$$#opredelenie
\vec{a} = \lim_{\Delta t \to 0} \frac{\Delta \vec{v}}{\Delta t}
$$

Предел записывается как производная скорости по времени:

$$#proizvodnaya
\vec{a} = \frac{d\vec{v}}{dt}
$$

Размерность сходится, скорость в м/с делится на время в с и получается м/с$^2$. Ускорение показывает, как быстро скорость увеличивается или уменьшается и как поворачивается её направление, ведь приращение $\Delta \vec{v}$ есть вектор, и лежать вдоль $\vec{v}$ он не обязан. Ускорение относится к скорости так же, как скорость относится к перемещению. Если скорость есть темп, с которым точка перемещается, то ускорение есть темп, с которым она меняет свою скорость.

## Продифференцируем столбец дважды

Скорость сама была производной радиус-вектора, значит ускорение есть производная от производной, вторая производная положения по времени:

$$#vtoraya
\vec{a} = \frac{d}{dt}\frac{d\vec{r}}{dt} = \frac{d^2 \vec{r}}{dt^2}
$$

Запишем это по осям. Радиус-вектор в декартовых осях есть столбец из трёх координат, каждая из которых зависит от времени, как записано в [[radius-vektor]], и каждая дифференцируется дважды:

$$#stolbec
\vec{r} = \begin{bmatrix} x(t) \\ y(t) \\ z(t) \end{bmatrix}, \qquad
\vec{v} = \begin{bmatrix} dx/dt \\ dy/dt \\ dz/dt \end{bmatrix}, \qquad
\vec{a} = \begin{bmatrix} d^2x/dt^2 \\ d^2y/dt^2 \\ d^2z/dt^2 \end{bmatrix}
$$

Право так писать даёт договорённость о неподвижных осях из [[sistema-otscheta]], уже в третий раз. Оси прикручены болтами, базисные векторы $\hat{i}, \hat{j}, \hat{k}$ от времени не зависят, и производная их не трогает, так что дифференцируются одни коэффициенты при них. Оси заведены один раз, а куплены за это покомпонентная производная, покомпонентный интеграл и теперь покомпонентная вторая производная. В осях, которые сами поворачиваются, так писать нельзя, потому что базисный вектор тогда меняется со временем и из-под производной не выносится. Чем это оборачивается, показано в [[normalnoe-i-tangencialnoe]], где оси посажены на движущуюся точку.

Посмотрим на примере. Пусть координаты точки в метрах и секундах заданы законом $x = 2t$, $y = 3t^2$, $z = 0$. Дифференцируем по столбцу: $\vec{v} = (2,\ 6t,\ 0)$ м/с, $\vec{a} = (0,\ 6,\ 0)$ м/с$^2$. Вдоль $x$ скорость постоянна и ускорения нет, вдоль $y$ ускорение постоянно и равно $6$ м/с$^2$; траектория при этом парабола $y = 3x^2/4$. Так по любому закону движения $\vec{r}(t)$ вычисляются $\vec{v}(t)$ и $\vec{a}(t)$, две производные, каждая покомпонентно.

## Пойдём обратно от ускорения к скорости

Обратная задача решается той же машиной, что в [[integrirovanie-skorosti]]. Ровно та же идея, только ступенью ниже. Если ускорение задано как функция времени, то на малом промежутке скорость меняется на $\Delta \vec{v} \approx \vec{a}(t)\,\Delta t$, а сумма таких изменений в пределе даёт интеграл:

$$#obratnyj
\vec{v}(t) - \vec{v}(t_0) = \int_{t_0}^{t} \vec{a}(t')\, dt'
$$

На графике зависимости ускорения от времени это площадь под кривой между $t_0$ и $t$. Площадь даёт изменение скорости, а не саму скорость. Чтобы получить $\vec{v}(t)$, нужна постоянная, скорость в начальный момент $\vec{v}(t_0)$, и интеграл её не знает, её измеряют. Второй такой же шаг, от скорости к положению, требует второй постоянной, $\vec{r}(t_0)$. Как это работает при постоянном ускорении и откуда в формуле для координаты берётся квадрат времени, показано в [[ravnouskorennoe-dvizhenie]].

Раз операция та же самая, её можно применить не только к радиус-вектору. У вектора скорости есть собственная траектория, и на ней ускорение при движении по окружности получается геометрией, без вычисления производных ([[godograf]]).
`);
