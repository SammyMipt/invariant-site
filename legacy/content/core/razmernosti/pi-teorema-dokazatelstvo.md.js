window.BLOCK(String.raw`
---
id: pi-teorema-dokazatelstvo
type: вывод
title: Доказательство Пи-теоремы
links: [pi-teorema, razmernaya-zavisimost]
---

**Шаг 1: Обезразмеривание переменных.** Выберем первые $k$ величин и обезразмерим все остальные:

$$#shag1
a = f(a_1, \ldots, a_k, a_{k+1}, \ldots, a_n) \Rightarrow \pi = f(a_1, \ldots, a_k, \pi_{k+1}, \ldots, \pi_n)
$$

где $\pi_i = \frac{a_i}{a_1^{b_1} \ldots a_k^{b_k}}$.

**Шаг 2: Изменение масштаба первой переменной.** Изменим масштаб величины $a_1$, но вид зависимости не изменится:

$$#shag2
\pi = f(a_1, \ldots, a_k, \pi_{k+1}, \ldots, \pi_n) \Rightarrow \pi = f(10a_1, \ldots, a_k, \pi_{k+1}, \ldots, \pi_n)
$$

Величины $a_1, \ldots, a_k$ взяты с независимыми размерностями, поэтому масштабы основных единиц можно подобрать так, чтобы число, изображающее $a_1$, выросло в десять раз, а числа для $a_2, \ldots, a_k$ остались прежними: ни одна из них не выражается через остальные, и масштабы меняются порознь. Безразмерные $\pi$ и $\pi_{k+1}, \ldots, \pi_n$ при смене единиц не меняются вовсе, а обе системы единиц лежат в одном классе, поэтому по условию теоремы вид $f$ тот же. Изменился один аргумент из всех, и десятка тут ничем не выделена: годится любой множитель.

Поскольку вид функции остаётся неизменным, можно сделать вывод, что функция не зависит от $a_1$, и её можно исключить:

$$
\pi = f(a_2, \ldots, a_k, \pi_{k+1}, \ldots, \pi_n)
$$

**Шаг 3: Повторение для остальных переменных.** Повторим аналогичное изменение масштаба для переменных $a_2, \ldots, a_k$. В результате остаются только безразмерные величины:

$$#shag3
\pi = f(\pi_{k+1}, \ldots, \pi_n)
$$

Это и есть пи-комплексы, которые выражают зависимость исходной величины через уменьшенное число независимых безразмерных параметров.
`);
