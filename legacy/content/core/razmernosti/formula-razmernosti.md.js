window.BLOCK(String.raw`
---
id: formula-razmernosti
type: вывод
title: Формула размерностей
links: [razmernaya-zavisimost, pi-teorema, formula-razmernosti-dokazatelstvo]
---

Кажется, что можно остановиться на том, что мы ввели основные единицы измерения и перейти к изучению конкретных физических величин. Однако, даже тот факт, что мы ввели единицы измерения, уже может нам помочь глубже понять физические процессы. Например, из введения основных единиц следует, что любая физическая величина может быть представлена как произведение базовых величин в определённых степенях.

**Утверждение:** Формула размерностей любой физической величины есть степенной одночлен от базовых размерностей — произведение их степеней, без сложения. В общем виде это утверждение можно записать как:

$$#statement
\left[ Q \right] = \text{L}^a \cdot \text{M}^b \cdot \text{T}^c
$$

где $\left[ Q \right]$ — размерность физической величины $Q$, а $a$, $b$, $c$ — показатели степеней, соответствующие длине (L), массе (M) и времени (T).

**Доказательство утверждения:** Будем доказывать это утверждение через преобразование масштабов величин. Рассмотрим функцию объёма $V$ и изменим её на примере двух объектов с линейными размерами $A = (a_1, a_2, a_3)$ и $B = (b_1, b_2, b_3)$. Предположим, что объём $V$ является функцией от размеров объекта: $V = f(A)$ и $V' = f(B)$.

**1. Однократное преобразование масштабов.** Выполним преобразование линейных размеров, увеличив их в $k$ раз. Тогда объёмы будут определяться как:

$$
V = f(A) \quad \text{и} \quad V' = f(B)
$$

после масштабирования:

$$
V = f(kA) \quad \text{и} \quad V' = f(kB)
$$

Получаем отношение:

$$
\frac{V}{V'} = \frac{f(A)}{f(B)} = \frac{f(kA)}{f(kB)}
$$

Здесь вступает второе допущение вывода: отношение $f(A)/f(kA)$ считается одним и тем же для любого тела, то есть зависит от масштаба $k$, но не от того, что именно мы меряем. Это постулат Бриджмена, проверяется он только опытом; без него формула размерностей не выводится.

$$#phi
\frac{f(A)}{f(kA)} = \frac{f(B)}{f(kB)} = \phi(k)
$$

**2. Свойства функции $\phi(k)$.** Учитывая, что при $k = 1$ объём не изменяется, получаем:

$$
\phi(1) = 1
$$

Кроме того, предположим, что у этой функции существует производная:

$$
\frac{d\phi(k)}{dk}\bigg|_{k=1} = N
$$

Мы используем это предположение для физических выкладок, так как в физике такие зависимости гладкие и имеют производные.

**3. Двойное преобразование масштабов.** Теперь рассмотрим двойное изменение масштаба:

$$#phi-mult
\phi(k_1 k_2) = \frac{f(A)}{f(k_1 k_2 A)} = \frac{f(A)}{f(k_1 A)} \cdot \frac{f(k_1 A)}{f(k_1 k_2 A)} = \phi(k_1) \cdot \phi(k_2)
$$

Это уравнение говорит о том, что функция $\phi$ должна удовлетворять функциональному уравнению, где $\phi(k_1 k_2) = \phi(k_1) \cdot \phi(k_2)$.

**4. Угадывание функции $\phi$.** Из этого уравнения мы можем угадать, что единственная функция, подходящая под данное условие, — это степень вида:

$$#phi-power
\phi(k) = k^N
$$

Угадать мало: надо убедиться, что других решений нет. Это делает [[formula-razmernosti-dokazatelstvo]].

**Следствия из утверждения**

**1. Умножение размерных величин:** Если $A$ и $B$ — две физические величины с размерностями $\left[ A \right] = \text{L}^a \cdot \text{M}^b \cdot \text{T}^c$ и $\left[ B \right] = \text{L}^d \cdot \text{M}^e \cdot \text{T}^f$, то их произведение $A \cdot B$ имеет размерность $\left[ A \cdot B \right] = \text{L}^{a+d} \cdot \text{M}^{b+e} \cdot \text{T}^{c+f}$.

**2. Возведение в степень:** Если физическая величина $A$ имеет размерность $\left[ A \right] = \text{L}^a \cdot \text{M}^b \cdot \text{T}^c$, то величина $A^n$ имеет размерность $\left[ A^n \right] = \text{L}^{an} \cdot \text{M}^{bn} \cdot \text{T}^{cn}$.
`);
